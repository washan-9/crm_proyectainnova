import { PlaceholderPage } from "@/components/placeholder-page";

export default function RecordatoriosPage() {
  return (
    <PlaceholderPage
      title="Recordatorios"
      description="Tareas y recordatorios pendientes asignados a cada usuario."
    />
  );
}
